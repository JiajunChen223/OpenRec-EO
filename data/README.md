# Data directory (draft)

Raw third-party data are intentionally absent from this scaffold. Follow `DATA_SOURCES.md` for provider links, versions, licences, and acquisition instructions. Keep restricted inputs outside version control and record only permitted checksums or anonymized schema metadata.
